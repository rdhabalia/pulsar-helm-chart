# add athenz n cameo changes

{{- if .Values.components.bookkeeper }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: "{{ .Values.bookkeeper.service_account.name }}"
  namespace: {{ template "pulsar.namespace" . }}
  labels:
    {{- include "pulsar.standardLabels" . | nindent 4 }}
    component: {{ .Values.bookkeeper.component }}
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.bookkeeper.service_account.annotations }}
{{- end }}
