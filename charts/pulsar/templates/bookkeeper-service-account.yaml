# serviceaccount and serviceaccount_configmap

{{ if .Values.components.bookkeeper }}
{{$data := dict
  "service_account" .Values.bookkeeper.service_account.name
  "component" .Values.bookkeeper.component
  "annotations" .Values.bookkeeper.service_account.annotations
}}
{{- include "pulsar.service-account" (merge (dict "Args" $data) . ) }}
---
{{$data := dict
  "service_account" .Values.bookkeeper.service_account.name
  "service_account_configmap" .Values.bookkeeper.service_account.configmap
  "component" .Values.bookkeeper.component
}}
{{- include "pulsar.service-account-configmap" (merge (dict "Args" $data) . ) }}
{{ end }}

