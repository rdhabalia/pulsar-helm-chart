## serviceaccount and serviceaccount_configmap
#
{{ if .Values.components.zookeeper }}
{{$data := dict
  "service_account" .Values.zookeeper.service_account.name
  "component" .Values.zookeeper.component
  "annotations" .Values.zookeeper.service_account.annotations
}}
{{- include "pulsar.service-account" (merge (dict "Args" $data) . ) }}
---
{{$data := dict
  "service_account" .Values.zookeeper.service_account.name
  "service_account_configmap" .Values.zookeeper.service_account.configmap
  "component" .Values.zookeeper.component
}}
{{- include "pulsar.service-account-configmap" (merge (dict "Args" $data) . ) }}
{{ end }}