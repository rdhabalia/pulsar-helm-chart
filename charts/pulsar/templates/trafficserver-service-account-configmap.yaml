# add athenz n cameo changes

{{- if .Values.components.trafficserver }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Values.trafficserver.service_account.configmap }}"
  namespace: {{ template "pulsar.namespace" . }}
  labels:
    {{- include "pulsar.standardLabels" . | nindent 4 }}
    component: {{ .Values.trafficserver.component }}
data:
  sia_config: |
    {
      "version": "1.0.0",
      "service": "{{ .Values.trafficserver.service_account.name }}",
      "sandns_wildcard": true,
      "accounts": [
        {
          "domain":  "{{ .Values.athenz.domain }}",
          "account": "{{ .Values.athenz.aws_account_id }}"
        }
      ]
    }
{{- end }}

